-- Gets all data different from each other from the "users" table
SELECT DISTINCT * FROM users;

-- Gets all the different values referring to the age attribute of the "users" table
SELECT DISTINCT age FROM users;
